#include <stdio.h>

enum Color
{
  RED,
  GREEN,
  BLUE
};

void color(int i);

int main()
{
  int color = RED;
  funColor(color);
  return 0;
}

void funColor(int i)
{
  char *color[3] = {"RED", "GREEN", "BLUE"};
  printf("%s", color[i]);
}